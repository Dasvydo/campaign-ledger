# campaign-ledger dependencies.
#
# The runtime has NONE. src/campaign_db.py talks to PostgREST over urllib from
# the standard library, on purpose: the ledger client is meant to be droppable
# into any batch repo without dragging a dependency tree behind it.
#
# sqlglot is test-only. tools/validate_sql.py parses the three migrations with
# its Postgres dialect so a syntax error is caught before anything is pasted
# into a SQL editor, and tests/run_local_proof.py asserts that parse as one of
# its 51 checks. Without it that single check errors and the gate reports 50/51.
#
#   pip install -r requirements.txt
#   python3 tests/run_local_proof.py     # 51 checks, the whole offline QA gate
#
# Pinned to the version the QA gate was last proved against (RUN-REPORT.md).
sqlglot>=30.18.0
